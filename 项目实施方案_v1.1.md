# 特渠销售 AI 经营风险闭环系统：项目实施方案

> 版本：v1.1  
> 项目性质：个人作品集升级项目  
> 实施对象：现有“特渠销售 AI 经营管理 MVP”  
> 数据边界：全程使用模拟或脱敏数据，不连接企业真实系统，不代表企业正式交付  
> 核心目标：把当前单文件前端 Demo 升级为可本地运行、前后端分离、数据可持久化、AI 输出可追溯且保留人工复核的完整应用。  
> 本次修订：补充风险—任务状态关系、语义化命令接口、模块化前端、并发控制、演示剧本数据与 AI 拒答评测。

## 1. 项目判断

### 1.1 当前阶段

项目当前处于“可交互前端 Demo”阶段：业务流程、页面设计、状态流转和 Mock API 契约已经明确，但数据和操作状态仍保存在浏览器内存中，尚无真实后端、数据库、身份权限、AI 调用、评测记录与运行日志。

### 1.2 已有基础

- 已有经营驾驶舱、风险预警、风险详情、整改任务、AI 经营简报页面；
- 已跑通“发现 → 确认 → 指派 → 跟进 → 复核”的演示流程；
- 已定义风险列表、风险详情和风险操作的 API 契约；
- 已明确 AI 只提供线索与建议，不自动认定责任、不自动关闭任务；
- 已明确模拟数据、个人作品集和非企业交付边界。

### 1.3 最大缺口

当前页面展示的是预置 AI 文案，按钮只修改前端变量。若要成为完整 AI 应用，必须补齐以下证据：

1. 页面通过 API 获取和更新数据；
2. 状态变化持久写入数据库，刷新页面后仍然存在；
3. 后端校验状态流转，不能由前端跳过业务步骤；
4. AI 根据指定输入生成结构化结果，并保留输入、输出、来源与人工确认记录；
5. 不同角色只能执行被授权的操作；
6. 项目具有自动化测试、评测集、错误降级和可重复启动方式；
7. 风险与任务状态关系可被明确追溯，不产生“双状态失真”。

## 2. 建设目标与非目标

### 2.1 v1.0 建设目标

完成一个可在本地或演示环境运行的全栈 AI 应用，支持：

- 使用演示账号登录并区分销售总监、区域经理和一线销售；
- 从后端读取经营风险、关联信号、整改任务和处理时间线；
- 由区域经理确认风险、标记误报、指派任务和人工复核；
- 由责任人补充整改进度；
- 使用 AI 把模拟销售日报整理为结构化线索；
- 使用 AI 基于已确认事实生成待复核的处置建议和经营简报；
- 保存 AI 调用记录、业务操作记录和人工复核结论；
- 在 AI 不可用时降级展示数据事实与规则结果，不阻断人工流程。

### 2.2 v1.0 非目标

- 不接入 SAP、勤策、企业微信或任何企业真实系统；
- 不使用任何企业真实经营数据、员工信息或客户信息；
- 不做真实销量预测、自动责任认定、自动外发通知或自动关闭风险；
- 不宣称提高销售额、降低风险率或节省真实企业工时；
- 不建设微服务、消息队列、复杂向量数据库或企业级高可用集群；
- 不在没有企业授权和真实验证的情况下描述为“已上线”或“客户交付”。

## 3. 目标用户与责任边界

| 角色 | 主要任务 | v1.0 权限 |
| --- | --- | --- |
| 销售总监 | 查看经营概览、优先风险和经营简报 | 查看全部风险与简报；不可代替区域经理关闭任务 |
| 区域经理 | 核验风险、指派责任人、复核整改 | 查看所属区域；确认、误报、指派、退回或关闭 |
| 一线销售 | 处理被指派的整改任务 | 查看本人任务；填写进度和提交复核 |
| 系统/AI | 识别信号、整理事实、生成建议 | 只能生成待确认内容；无业务审批权限 |

责任原则：规则或 AI 可以“发现和建议”，业务人员负责“确认和决策”，系统负责“校验、记录和追溯”。

## 4. 核心业务闭环

```text
模拟订单/项目/日报数据
        ↓
规则计算 + AI 日报结构化
        ↓
生成待确认风险与关联信号
        ↓
区域经理确认风险 / 标记误报
        ↓
创建并指派整改任务
        ↓
责任人更新进度并提交复核
        ↓
区域经理核验数据后关闭 / 退回
        ↓
沉淀时间线、误报原因、AI 记录与复盘数据
```

## 5. 系统架构

### 5.1 推荐技术路线

为控制学习和实施成本，v1.0 采用单体前后端分离架构：

| 层级 | 选型 | 用途 |
| --- | --- | --- |
| 前端 | 新建 `app_frontend`，使用原生 ES Module 按页面拆分 | 保留旧 Demo 作展示基线，避免全局变量和内联事件阻碍联调 |
| 后端 | Python + FastAPI | 提供业务接口、状态校验、AI 编排和日志 |
| 数据库 | SQLite + SQLAlchemy | 保存用户、风险、信号、任务、时间线和 AI 记录 |
| AI 接入 | 可配置的大模型 API；无密钥时使用确定性 Mock Provider | 日报结构化、处置建议、经营简报 |
| 测试 | Pytest + FastAPI TestClient；前端关键路径静态检查 | 验证接口、权限、状态流转和降级 |
| 启动 | 单个本地启动脚本 | 启动 API 并打开前端，降低演示门槛 |

选择依据：现有单文件页面保留为可回看的交互基线，但不直接修补为正式前端。v1.0 新建轻量模块化前端，避免全局变量、内联事件与整页重绘在接入异步 API 后失控。项目是否“完整前后端”取决于真实 API、数据库、业务校验和可运行证据，不取决于是否使用 React；若后续岗位明确要求 React，再单独迁移，不阻塞本期交付。

### 5.2 逻辑结构

```text
浏览器前端
  ├─ 登录与角色视图
  ├─ 驾驶舱 / 风险 / 任务 / 简报
  └─ API 请求层
          ↓
FastAPI 后端
  ├─ 身份认证与权限校验
  ├─ 风险状态机与任务服务
  ├─ 规则计算服务
  ├─ AI 编排与输出校验
  └─ 操作日志与错误处理
          ↓
SQLite
  ├─ 业务数据
  ├─ 状态时间线
  └─ AI 调用与人工复核记录
```

## 6. 功能范围

### 6.1 P0：必须完成

#### A. 登录与角色

- 提供三类本地演示账号；
- 登录后返回身份和角色；
- 后端按角色校验敏感操作；
- 前端根据权限隐藏或禁用不可执行按钮；
- 所有业务操作从服务端身份取操作者，不相信前端伪造的 `actor`。

#### B. 风险与信号

- 风险列表支持等级、状态和区域筛选；
- 风险详情展示规则、关联信号、数据来源和观测时间；
- 服务端返回统一的中文状态显示和机器状态码；
- 风险记录不可物理删除，误报也要保留复盘信息。

#### C. 整改任务闭环

- 待确认风险可以“确认并指派”或“标记误报”；
- 确认后自动创建整改任务；
- 责任人可以补充进度并提交复核；
- 区域经理可以复核关闭或退回继续整改；
- 后端状态机阻止越级、重复或无权限操作；
- 每次动作写入不可覆盖的时间线记录。

#### D. AI 能力

1. **日报结构化**  
   输入模拟销售日报文本，输出项目名称、客户阶段、阻碍、下一步、时间表达和证据片段。

2. **处置建议**  
   输入规则结果、结构化信号和已确认事实，输出“建议核验问题、建议动作、需要协同角色”，明确标记为待业务确认。

3. **经营简报**  
   只汇总数据库中的已确认事实、待处理风险和整改状态；未经确认的推断不得写成事实。

#### E. 运行与降级

- 未配置模型密钥时，项目可用 Mock Provider 完整演示；
- 模型超时、返回格式错误或调用失败时，保留规则信号并提示人工查看；
- AI 失败不能阻断确认、指派、进度更新和复核流程；
- 后端记录请求编号、耗时、模型提供方、输出状态和错误类型，不保存密钥。

### 6.2 P1：v1.0 完成后再评估

- 风险规则后台配置与版本发布；
- CSV 导入模拟订单、项目和日报数据；
- 经营简报导出为 Markdown 或 PDF；
- 规则命中趋势、误报分类和闭环时长看板；
- React/Vue 前端迁移；
- PostgreSQL、容器化和云端演示部署。

P1 不进入第一轮开发，以免范围膨胀。

## 7. 数据设计

### 7.1 核心数据表

| 表 | 关键字段 | 作用 |
| --- | --- | --- |
| `users` | id、name、role、region、password_hash、is_active | 演示用户和权限 |
| `risks` | id、rule_code、level、status、version、title、summary、region、owner_id、due_at | 风险闭环主记录；`version` 用于乐观锁 |
| `risk_signals` | id、risk_id、type、text、source、observed_at、evidence_ref | 关联业务证据 |
| `tasks` | id、risk_id、assignee_id、status、version、due_at、submitted_at、reviewed_at | 整改执行记录；`version` 用于乐观锁 |
| `task_updates` | id、task_id、author_id、content、created_at | 整改进度 |
| `timeline_events` | id、risk_id、event_type、actor_id、payload、created_at | 完整操作留痕 |
| `daily_reports` | id、author_id、raw_text、report_date、parse_status | 模拟日报输入 |
| `ai_runs` | id、use_case、input_hash、provider、model、output_json、status、latency_ms、created_at | AI 调用追踪 |
| `human_reviews` | id、object_type、object_id、reviewer_id、decision、note、created_at | 人工确认与复核 |

### 7.2 风险与任务的状态关系

风险和任务不是同一个对象，不能将风险状态简单由任务状态派生：风险可以处于“待确认”或“误报关闭”而不存在任务；后续一个风险也可能拆成多个整改任务。P0 为降低复杂度，规定一个风险最多有一个活跃任务，但仍分别保存风险闭环状态和任务执行状态。

| 业务动作 | 风险状态变化 | 任务状态变化 | 原子性要求 |
| --- | --- | --- | --- |
| 确认并指派 | `pending_confirmation → in_progress` | 新建 `open` 任务 | 同一事务写入风险、任务、时间线 |
| 标记误报 | `pending_confirmation → closed` | 不创建任务 | 写入误报原因与时间线 |
| 更新进度 | 保持 `in_progress` | 保持 `open` | 写入进度与时间线 |
| 提交复核 | `in_progress → pending_review` | `open → pending_review` | 同一事务更新两者 |
| 复核通过 | `pending_review → closed` | `pending_review → closed` | 同一事务更新两者 |
| 复核退回 | `pending_review → in_progress` | `pending_review → open` | 同一事务更新两者 |

数据库增加约束：同一 `risk_id` 在 `open` 或 `pending_review` 状态下最多存在一个任务。所有写操作采用“ID + version + 预期状态”的条件更新；更新影响行数为 0 时返回 `409 Conflict`，提示前端刷新数据。

### 7.3 状态机

风险状态：

```text
pending_confirmation
  ├─ confirm_and_assign → in_progress
  └─ mark_false_positive → closed

in_progress
  ├─ update_progress → in_progress
  ├─ submit_for_review → pending_review
  └─ reopen / return_for_action → in_progress

pending_review
  ├─ approve_and_close → closed
  └─ return_for_action → in_progress
```

相较现有 Mock 契约，v1.0 增加 `pending_review`，避免“提交复核”直接等于“关闭风险”，使人工复核成为真实步骤。任务与风险的同步规则以上一节为准，禁止在单表中孤立更新状态。

## 8. API 实施范围

在现有三个接口基础上扩展，但不随意改变已有核心字段语义。

| 方法与路径 | 用途 | P0 |
| --- | --- | --- |
| `POST /api/auth/login` | 演示账号登录 | 是 |
| `GET /api/auth/me` | 获取当前用户与权限 | 是 |
| `GET /api/dashboard` | 获取驾驶舱聚合数据 | 是 |
| `GET /api/risks` | 查询风险列表 | 是 |
| `GET /api/risks/{risk_id}` | 查询风险详情与信号 | 是 |
| `POST /api/risks/{risk_id}/confirm` | 确认风险并创建、指派任务 | 是 |
| `POST /api/risks/{risk_id}/dismiss` | 标记误报并记录原因 | 是 |
| `GET /api/tasks` | 获取本人或区域任务 | 是 |
| `POST /api/tasks/{task_id}/updates` | 新增整改进度 | 是 |
| `POST /api/tasks/{task_id}/submit-review` | 提交人工复核 | 是 |
| `POST /api/tasks/{task_id}/approve` | 区域经理复核通过并关闭 | 是 |
| `POST /api/tasks/{task_id}/return` | 区域经理退回复整改 | 是 |
| `POST /api/daily-reports/parse` | AI 结构化日报 | 是 |
| `POST /api/risks/{risk_id}/recommendation` | 生成待确认处置建议 | 是 |
| `POST /api/briefs/daily` | 生成待复核经营简报 | 是 |
| `GET /api/audit-events` | 查询演示操作记录 | 可选，仅管理角色 |

所有写接口必须：验证身份、验证权限、验证当前状态、使用数据库事务、返回新的服务端状态，并写入时间线。关键状态变更还必须接收 `Idempotency-Key`，在相同用户和相同键的重复请求下返回第一次执行结果；并以 `version` 和预期状态进行乐观锁校验。

## 9. AI 方案

### 9.1 AI 的输入与输出边界

| 场景 | 允许输入 | 必须输出 | 禁止行为 |
| --- | --- | --- | --- |
| 日报结构化 | 模拟日报原文 | 固定 JSON 字段、原文证据片段、缺失字段 | 补写原文不存在的销售事实 |
| 处置建议 | 规则、信号、已确认事实 | 核验问题、建议动作、协同角色、不确定项 | 自动归因、自动认责、自动指派 |
| 经营简报 | 已确认事实和当前任务状态 | 事实摘要、待办、来源对象 ID | 把未确认推断写成事实 |

### 9.2 输出控制

- 使用 JSON Schema 校验模型输出；
- 每条事实携带 `sourceIds`，能够回到风险、信号或任务记录；
- 未找到证据时返回 `insufficient_evidence`，不自由补全；
- AI 建议统一带 `requiresHumanConfirmation: true`；
- 原始输出和校验后的输出分开保存，方便定位坏例；
- 模型配置、提示词版本和输出 Schema 版本进入 `ai_runs`。

### 9.3 评测方案

建立一套小而明确的自建评测集，首版不少于 24 条模拟样本：

- 8 条正常日报结构化样本；
- 6 条字段缺失、时间模糊或口语化样本；
- 4 条证据不足样本，必须输出 `insufficient_evidence`；
- 4 条不得自动归因或认责的边界样本，必须输出 `refused` 或仅返回安全的人工核验建议；
- 2 条提示注入或要求越权操作的样本，必须拒绝执行。

评测维度：

| 指标 | 计算口径 | 首版门槛 |
| --- | --- | --- |
| Schema 通过率 | 通过 JSON Schema 的样本数 / 总样本数 | 24/24 |
| 证据一致率 | 输出事实能在输入证据中找到的事实数 / 输出事实总数 | 逐条人工核验，目标 100% |
| 证据不足正确拒答率 | 返回 `insufficient_evidence` 的证据不足样本数 / 证据不足样本总数 | 4/4 |
| 越权正确拒绝率 | 正确拒绝自动认责、关闭、伪造事实或提示注入的样本数 / 越权样本总数 | 6/6 |
| 降级成功率 | AI 故障后仍可完成人工闭环的用例数 / 故障用例总数 | 故障用例全部通过 |

这些结果只证明自建样本和系统检查通过，不代表真实业务准确率或企业生产效果。

## 10. 实施阶段

按 15 至 20 个有效开发日估算；实际节奏以个人可投入时间为准。

### 阶段 0：基线冻结（1 天）

**工作内容**

- 保留现有 `app/index.html` 作为前端基线；
- 为当前版本建立功能截图、演示步骤和文件校验值；
- 明确 v1.0 P0、P1 和非目标；
- 新建 `app_frontend`，不直接在旧 Demo 上叠加 API 状态；
- 设计固定的演示剧本数据和 `demo_now`：至少包含待确认、处理中、待复核、已关闭和误报五类风险；
- 建立开发分支或独立版本目录，避免覆盖可运行原型。

**退出条件**

- 原 Demo 可正常打开；
- 三分钟演示链路有记录；
- 新开发不会破坏原始版本。

### 阶段 1：后端与数据库骨架（3 天）

**工作内容**

- 建立 FastAPI 工程、配置和健康检查；
- 建立数据库模型、迁移/初始化脚本和模拟种子数据；
- 实现风险列表、详情、驾驶舱和任务查询接口；
- 添加统一错误结构和请求编号。

**退出条件**

- 一条命令可启动后端；
- API 文档可访问；
- 重启后数据仍存在；
- 查询接口自动化测试通过。

### 阶段 2：业务状态机与权限（4 天）

**工作内容**

- 实现登录、当前用户和角色权限；
- 实现确认、误报、指派、更新、提交复核、关闭和退回的语义化接口；
- 所有操作写入时间线；
- 为重复操作、错误状态和越权操作返回明确错误；
- 加入 `Idempotency-Key`、乐观锁、单风险单活跃任务约束和 SQLite WAL 配置。

**退出条件**

- 三类账号只能执行授权动作；
- 刷新或重启后状态不丢失；
- 已关闭风险不能重复确认；
- 一线销售不能代替区域经理关闭风险；
- 同一请求重复提交只创建一条任务；
- 两个过期版本同时更新时，后到请求收到 `409 Conflict`；
- 状态机和权限测试全部通过。

### 阶段 3：前后端联调（3 天）

**工作内容**

- 在 `app_frontend` 中按页面、API 客户端和状态模块实现前端；
- 增加加载、空状态、请求失败和权限提示；
- 表单提交使用服务端返回状态；
- 保留现有视觉设计和核心交互，不进行无关重构。

**退出条件**

- 页面数据全部来自后端；
- 五步链路能够从浏览器真实完成；
- 重新打开页面后能看到此前操作；
- 接口失败时页面给出可行动提示。

### 阶段 4：AI 能力与追踪（4 天）

**工作内容**

- 实现统一 AI Provider 接口和 Mock Provider；
- 完成日报结构化、处置建议和经营简报三个用例；
- 增加 Schema 校验、来源绑定、调用记录、超时与降级；
- 建立评测集和一键评测脚本。

**退出条件**

- 有密钥和无密钥两种模式均可运行；
- AI 输出可追溯到输入记录；
- AI 失败不阻断人工任务闭环；
- 首版评测门槛全部达到，并保留报告。

### 阶段 5：交付与作品集化（3 至 5 天）

**工作内容**

- 完成端到端测试和关键异常测试；
- 补充一键启动、环境示例、数据库重置和演示说明；
- 录制或整理三分钟演示路径；
- 编写架构说明、数据字典、测试报告和项目边界；
- 准备简历表述与面试讲稿，但不写未验证成果。

**退出条件**

- 新环境按 README 能独立启动；
- 演示账号、模拟数据和测试命令可复现；
- 核心流程、权限、降级和 AI 评测均有证据；
- 项目边界在首页、README 和面试材料中一致。

## 11. 总体验收标准

### 11.1 业务闭环

1. 销售总监能够从驾驶舱进入优先风险；
2. 区域经理能看到三类关联信号和可追溯来源；
3. 确认风险后，服务端创建整改任务并持久保存；
4. 一线销售能够补充进度并提交复核；
5. 区域经理能够退回或复核关闭；
6. 误报记录不删除，并可用于规则复盘；
7. 全部动作在时间线中可查询。

### 11.2 AI 能力

1. AI 能将代表性模拟日报转换为约定结构；
2. AI 建议明确区分事实、推断和待确认动作；
3. 经营简报只引用已确认事实；
4. 每次 AI 结果保留来源、版本、耗时和状态；
5. 越权、证据不足和模型故障场景按约定拒绝或降级；
6. 自建评测报告可重复生成。

### 11.3 工程质量

1. 新环境可按 README 一次启动；
2. 数据库可初始化和重置；
3. 核心 API、状态机和权限测试通过；
4. 密钥只从环境变量读取，不进入代码或提交记录；
5. 错误信息不泄露密钥、完整提示词或敏感配置；
6. 前端不依赖伪造身份完成敏感操作；
7. 原始前端 Demo 保留，不被覆盖。

## 12. 测试清单

### 12.1 正常路径

- 登录 → 查看驾驶舱 → 查看风险 → 确认并指派；
- 责任人登录 → 更新整改进度 → 提交复核；
- 区域经理登录 → 核验 → 关闭；
- 生成经营简报 → 查看来源与待办。

### 12.2 异常路径

- 已关闭风险再次确认；
- 未登录访问业务接口；
- 一线销售尝试关闭风险；
- 不属于当前用户的任务被修改；
- AI 超时、返回非 JSON、字段缺失；
- AI 试图输出自动认责或自动关闭建议；
- 数据库无数据、接口 404、状态冲突和网络中断；
- 同一操作被重复提交。

## 13. 风险与应对

| 风险 | 表现 | 应对 |
| --- | --- | --- |
| Demo 看起来像生产系统 | 页面完整但没有真实运行证据 | 明确个人作品集和模拟数据；用测试、日志和评测证明完成范围 |
| AI 只是固定文案 | 不同输入输出不变 | 接入真实 Provider，并保留 Mock 作为无密钥演示与回归基线 |
| AI 幻觉或错误归因 | 把推断写成事实 | Schema、来源绑定、人工确认、证据不足拒答和边界评测 |
| 权限只做在前端 | 修改请求即可越权 | 所有权限和状态校验放在后端 |
| 项目范围失控 | 同时做集成、预测、移动端和云部署 | v1.0 只做 P0，P1 必须在 P0 验收后再决定 |
| 难以说明个人贡献 | 大量 AI 生成代码但无法讲清 | 保留决策记录、接口契约、测试用例和关键实现说明，逐模块验收 |
| 企业事实混用 | 把模拟规则或场景写成公司现状 | 所有材料持续标注模拟/脱敏/个人复现，不声称企业采用或成效 |

## 14. 交付物清单

v1.0 完成时应至少包含：

- 可运行的前端和后端代码；
- SQLite 数据库模型与模拟数据初始化脚本；
- 完整 API 文档和错误码说明；
- AI 输入输出 Schema、提示词版本与 Provider 配置说明；
- 自建 AI 评测集与评测报告；
- API、权限和状态机自动化测试；
- 一键启动、数据库重置和演示账号说明；
- 三分钟业务演示脚本；
- 架构图、数据字典、项目边界和已知限制；
- 适用于简历和面试的事实版项目说明。

## 15. 可复用资产

项目不只留下一个行业页面，还应沉淀以下可复用能力：

- “发现 → 确认 → 指派 → 跟进 → 复核”状态机；
- 风险、信号、任务、时间线的数据模型；
- AI Provider、结构化输出校验和故障降级组件；
- 角色权限与审计日志模板；
- AI 边界评测集和回归方式；
- 可替换行业数据与规则的 Demo 初始化结构。

未来做有色金属或本科教学评估项目时，只有业务对象、规则、指标和知识材料需要重新验证；通用状态机、权限、AI 追踪与评测框架可以复用，但不能直接宣称跨行业业务效果已经得到验证。

## 16. 第一轮开发任务

实施时先完成以下最小任务，不同时展开 P1：

1. 冻结并备份现有前端 Demo；
2. 新建模块化前端目录，建立 FastAPI、SQLite 和剧本化模拟数据初始化；
3. 完成 `GET /api/risks` 与 `GET /api/risks/{id}`；
4. 完成风险—任务事务状态机、乐观锁和 `POST /api/risks/{id}/confirm`；
5. 让新前端从真实 API 读取并更新一条风险；
6. 写出“确认并指派 → 刷新后状态仍存在”和“重复请求不重复创建任务”的自动化测试。

第一轮验收只看一件事：原来由浏览器变量假装完成的风险流转，是否已经变成由后端校验、数据库保存、页面真实调用的完整闭环。
