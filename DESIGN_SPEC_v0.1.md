# 特渠销售 AI 经营管理 MVP：页面设计规范

> 版本：v0.1  
> 适用范围：当前 MVP 的经营驾驶舱、风险预警、风险详情、整改任务与经营简报。  
> 目的：让设计、前端和产品围绕同一套页面规则协作，而不是凭各自感觉改页面。

## 1. 设计目标

### 页面服务谁？

区域经理和特渠销售管理者。

### 页面只解决一个问题

让用户在 30 秒内判断：**哪条经营风险最需要先处理，以及下一步由谁做什么。**

### 设计取舍

- 不做炫技型大屏，不用无关的渐变、地图或装饰图表；
- 数据很多时，优先展示“需要动作的风险”，不把所有指标同等放大；
- 红色和黄色只表示风险等级，不能作为普通装饰色；
- AI 输出必须带“待业务确认”字样，不能伪装成确定事实。

## 2. 页面骨架

```text
左侧：我在哪个模块
顶部：当前页面 + 数据更新时间
正文：今天最需要处理的风险
下方：支持判断的数据、线索和具体动作
```

### 记忆点：优先风险横幅

在经营驾驶舱顶部使用一条深绿色横幅，只放一件事：

> 发现 1 项需要今日确认的经营风险 → 查看风险

它不是装饰，而是把“数据发现”直接带到“管理动作”的入口。页面其他区域保持安静，避免用户被卡片和颜色分散注意力。

## 3. 颜色规范

| 名称 | 色值 | 用在哪 | 不该用在哪 |
| --- | --- | --- |
| 松林绿 | `#174D40` | 主按钮、侧栏、关键横幅 | 大段正文、风险等级 |
| 协同绿 | `#2D7865` | 已完成步骤、可点击文字、来源线索 | 警告或错误 |
| 米白底 | `#F7F7F2` | 页面底色 | 卡片文字底色 |
| 墨绿字 | `#1D2B28` | 标题、重要数字 | 次要说明 |
| 提示黄 | `#C9791F` | 一般风险、当前进行中的步骤 | 普通按钮、趋势图装饰 |
| 风险红 | `#B64036` | 严重风险、不可逆错误 | 普通强调、所有负向数字 |

## 4. 字体与信息层级

| 内容 | 字体与大小 | 规则 |
| --- | --- | --- |
| 页面标题 | Georgia + 微软雅黑，26px | 只说当前用户要做的事，例如“先处理能影响本月结果的风险” |
| 卡片核心数字 | Georgia，28px | 只放可行动的核心指标，例如待确认风险数 |
| 区块标题 | 微软雅黑，15px，加粗 | 不使用泛泛名称，如“数据分析” |
| 正文 | 微软雅黑，14px | 用完整但短的业务句子 |
| 辅助说明 | 微软雅黑，12px，灰绿 | 说明数据时间、来源或边界 |

## 5. 间距与组件规则

### 页面布局

- 左侧导航：228px；
- 顶部栏：72px；
- 页面左右内边距：32px；
- 主区块之间：18px；
- 卡片内部：16px；
- 卡片圆角：9px；
- 阴影只用于悬浮或关键横幅，普通卡片使用边框即可。

### 按钮

| 类型 | 样式 | 示例 |
| --- | --- | --- |
| 主操作 | 松林绿底、白字 | 确认风险并指派 |
| 次操作 | 白底、灰绿边框 | 返回预警中心、补充整改进度 |
| 文字操作 | 无底色、协同绿文字 | 查看处置、查看全部 |
| 危险操作 | 不用红色实心主按钮 | 标记为误报需二次确认 |

按钮文案使用“动作 + 对象”，例如“提交人工复核”，不要写“确定”“提交”这种没有上下文的词。

## 6. 风险状态展示规则

| 业务状态 | 页面状态 | 用户能做什么 |
| --- | --- | --- |
| 系统发现异常 | 待确认 | 查看信号、确认并指派、标记误报 |
| 管理者确认需要处理 | 处理中 | 补充整改进度、提交人工复核 |
| 人工复核完成 / 确认为误报 | 已关闭 | 查看处置记录和复盘 |

同一条风险在列表、详情、任务中心中必须使用同一个状态词；不能在一个页面写“待处理”，另一个页面写“处理中”。

## 7. AI 内容展示规则

AI 相关内容只使用两种样式：

1. **数据线索**：淡绿色底 + 来源说明。例如“近 3 日日均出货额较前一周下降 24%”。
2. **AI 处置建议（待业务确认）**：白底边框区块，必须说明建议不是自动结论。

禁止出现：

- “系统认定某员工存在履职问题”；
- “AI 已确认问题原因”；
- “一键自动关闭风险”。

## 8. 空状态、错误与反馈

| 情况 | 页面应该怎么说 |
| --- | --- |
| 当前筛选没有风险 | “当前筛选条件下没有风险。查看全部风险” |
| 数据暂未更新 | “当前数据尚未完成更新，请在更新后重新查看。” |
| 关闭风险前未完成复核 | “请先完成数据核验，再提交人工复核。” |
| 操作成功 | “整改任务已创建并通知区域负责人。” |

不使用“操作失败”“未知错误”这类没有下一步的提示。

## 9. 前端落地检查清单

- [ ] 页面标题是否说明用户当前最重要的任务；
- [ ] 风险红黄是否只用于风险；
- [ ] 每个按钮是否有明确动作和结果；
- [ ] AI 建议是否明确写了“待业务确认”；
- [ ] 三个页面中的同一风险状态词是否一致；
- [ ] 小屏幕时侧栏隐藏、主内容单列展示；
- [ ] 键盘聚焦时能看清当前操作位置；
- [ ] 不是靠颜色单独表达风险等级，文字也应写明“严重风险/一般风险”。
